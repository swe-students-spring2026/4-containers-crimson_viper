name: Web App Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"

      - name: Install Pipenv
        run: |
          python -m pip install --upgrade pip
          python -m pip install pipenv

      - name: Remove broken env if present
        run: |
          pipenv --rm || true

      - name: Install dependencies
        env:
          PIPENV_IGNORE_VIRTUALENVS: "1"
        run: |
          pipenv --python 3.9
          pipenv install --dev

      - name: Verify pytest is installed
        run: |
          pipenv run python -m pip show pytest
          pipenv run python -m pytest --version

      - name: Run tests
        env:
          PYTHONPATH: .
        run: |
          pipenv run python -m pytest --cov=. --cov-report=term-missing